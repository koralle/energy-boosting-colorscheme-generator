import type { Meta, StoryObj } from "@storybook/react-vite";

const meta: Meta = {
  title: "デザインシステム / カラーパレット",
  component: () => <></>,
};

export default meta;
type Story = StoryObj;

export const Default: Story = {};
